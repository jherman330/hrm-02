# Task Management System Dependencies

# Web Framework
Flask>=3.0.0
flask-cors>=4.0.0

# Data validation
pydantic>=2.0.0

# Production WSGI server (optional, for deployment)
gunicorn>=21.0.0; sys_platform != 'win32'
waitress>=2.1.0; sys_platform == 'win32'
